# Master Imperfect Application

Committed SPEC §48 acceptance target. It deliberately combines a TypeScript web
frontend, Python API, PostgreSQL, Redis, Docker Compose, authentication, an administrator
surface, file upload, OpenAPI, MCP, and a CLI with controlled defects. It is not a sample
application and must never be deployed.
