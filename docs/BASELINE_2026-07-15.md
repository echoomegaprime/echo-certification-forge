# Certification Forge Baseline — 2026-07-15

## Confirmed

- Canonical local repository was absent at the start of this pass.
- Canonical private GitHub repository was absent at the start of this pass.
- The repository is now private at `ECHO-OMEGA-PRIME/echo-certification-forge` and cloned at the canonical path.
- The full 76-page source specification is vendored in `docs/SPEC.md` with source SHA-256 `09cd016d775095b0f543e62717a41455deb17f54ee187039f05ac47d147b1e78`.
- Echo Desktop exists but was not modified; its worktree contained 22 tracked changes and 358 untracked files during baseline inspection.
- The parent `C:\ECHO_OMEGA_PRIME` worktree contained 59 tracked changes and 878 untracked files; this repository was created separately to preserve them.
- The local skill runtime contains and successfully executed skills 30, 31, 32, 33, 34, 39, and 44 with provenance.
- The live SDK registry did not expose the requested `echo.skills.*` aliases or any `echo.certforge.*` capabilities.
- ANVIL Family server `http://192.168.1.49:8200` was reachable and loaded `echo-prime`, `echo-gs343`, `echo-gs343-v2`, and `echo-r2d2`.

## Existing project-task status

The project handoff records tasks `#1`, `#7`, `#8`, `#9`, `#13`, `#16`, `#23`, and `#25` as completed outside this repository. Relevant remaining work includes GS343 qualification (`#10`), R2D2 follow-ups (`#19`), Forge release certification (`#24`), and product media (`#26`). Those historical labels are not treated as proof of current runtime health.

## Missing at baseline

- control plane and persistence;
- immutable target/environment contracts;
- deterministic evidence and verdict authority;
- signing and public verification;
- isolated runner and authenticated control channel;
- adapter registry and conformance kits;
- GS343 and R2D2 measured qualification evidence;
- tenant retention/export/deletion and subscriber entitlements;
- external evidence-root anchoring;
- SDK capability registration;
- deployment and Echo Desktop P8C integration.

## Priority order

1. Deterministic core contracts and default-block evidence gate.
2. Signed verdict lifecycle and exact-digest deployment gate.
3. Ephemeral runner isolation and authenticated worker protocol.
4. Hostile-target, failure, recovery, and tenant-isolation tests.
5. Adapter families and GS343/R2D2 qualification.
6. Stable service/API/SDK capabilities.
7. Subscriber productization and independent release certification.
8. Echo Desktop P8C control surface.
